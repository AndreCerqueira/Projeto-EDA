/*****************************************************************//**
 * \file   MeioMobilidade.c
 * \brief  Meio de Mobilidade functions
 * 
 * \author A. Cerqueira
 * \date   February 2023
 *********************************************************************/

#include "MeioMobilidade.h"
#include <cstdio>
/*
void SaveDataToFile(MeioMobilidade* meios, int size) {
	FILE* file;
	fopen_s(&file, SAVE_FILE_NAME, "wb");

	if (file == NULL)
		return;

	fprintf(file, "Average distance: %f\n", average);
	fprintf(file, "Max distance: %f\n", max);
	fprintf(file, "Min distance: %f\n", min);

	for (int i = 0; i < size; i++)
		fprintf(file, "%f\n", distances[i]);

	fclose(file);
}
*/
